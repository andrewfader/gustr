=simple_form_for @image do |f|
  =f.input :user_id, input_html: { value: @user_id }, as: :hidden
  =f.input :upload, label: "Select Photo"
%p
%ul.images
  -@images.reverse.each_with_index do |image, index|
    %li.image
      .imagebox
        =link_to image_tag(image.upload.small.url), image_path(image)
        .floater.visible{data: {width: image.width, height: image.height, left: image.posX, top: image.posY + index*555+50}}
          .caption
            =image.caption.try(&:html_safe)
