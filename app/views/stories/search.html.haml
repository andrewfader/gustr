%table.datatable
  %thead
    %tr
      %th Storybook
      %th Genre
      %th Chapter
      %th City
      %th Tags
      %th By
  - Story.published.each do |story|
    %tr
      %td= story.storybook
      %td= story.genre
      %td= link_to story.place_name.split(',')[0], story
      %td= story.city
      %td= story.tags.map(&:tag).map(&:titleize).to_sentence
      %td= story.user.name
